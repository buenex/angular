import { ProdutoComponent } from './produto.component';
import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class ProdutoService {

  constructor() { }

  inserir() {
    // this.http.post(this.juridicoUrl, JSON.stringify(JuridicoComponent.call('getClass()')));
   }
}
